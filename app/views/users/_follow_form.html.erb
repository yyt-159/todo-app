<div class="col-12 my-2">
<% unless current_user?(@user) %>
  <div id="follow_form">
  <% if @current_user.following?(@user) %>
    <%= render 'unfollow' %>
  <% else %>
    <%= render 'follow' %>
  <% end %>
  </div>
<% end %>
</div>